services:
  postgres:
    image: postgres:16
    container_name: habit-tracker-db
    environment:
      POSTGRES_DB: habittracker
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"